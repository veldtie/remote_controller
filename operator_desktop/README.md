# Operator Desktop

PyQt6 desktop wrapper for the web operator UI.

**Install**
1. Create and activate a virtual environment.
2. `pip install -r requirements.txt`

**Run**
- `python app.py`

**Build**
- `build_windows_silent.bat`
