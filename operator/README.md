# Operator Web UI

Static web console for managing sessions. It is also embedded in the desktop operator app.

**Run**
- Open `index.html` directly, or serve the `operator/` folder with any static server.
