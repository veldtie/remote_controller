# Общие зависимости для client + operator_desktop + server.
# Можно ставить одной командой: pip install -r requirements.txt

# Client
aiortc
av
cryptography
geoip2
mss
numpy
pynput; platform_system == "Windows"
pywin32; platform_system == "Windows"
sounddevice
websockets>=12,<13

# Operator desktop
PyInstaller
PyQt6>=6.5
PyQt6-WebEngine>=6.5
requests>=2.31

# Server
asyncpg
fastapi
uvicorn
